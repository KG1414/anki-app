import React from 'react';
import Cards from '../Cards/Cards';

const Layout = () => {
    return (
        <div>
            <h1>Layout</h1>
            <Cards />
        </div>
    );
};

export default Layout;